{% extends "base.twig" %}

{% block body %}
    <h1>Новое сообщение</h1>

    {% if errors is not empty %}
        <div style="color: red">При сохранении произошла ошибка</div>
        <ul>
            {% for error in errors %}
                <li>
                    {{ error }}
                </li>
            {% endfor %}
        </ul>
    {% endif %}
    <form action="/mails" method="post">
        <div>
            <label>
                Сообщение *
                <input type="text" name="mail[letter]" value="{{ data.title ?? '' }}">
                {% if errors.title %}
                    <div style="color: red">{{ errors.title }}</div>
                {% endif %}
            </label>
        </div>
        <br>
        <br>
        <input type="submit" value="Create">
    </form>
{% endblock %}
